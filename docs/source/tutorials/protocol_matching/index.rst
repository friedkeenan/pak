Matching a Packet Protocol Using Pak
=======================================

In these documents we will create :class:`.Packet`\s and :class:`.Type`\s in order to match an invented packet protocol. We will tweak the protocol as we go along, exploring features of Pak to compensate for the changes.

.. toctree::

    basic
    stringy
    context
    identifying
    handle
